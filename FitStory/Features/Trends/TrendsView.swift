import SwiftUI

struct TrendsView: View {
    var body: some View {
        VStack {
            Text("Track your progress!")
                .font(.title)
                .padding()
            Text("This is the Trends screen.")
        }
    }
}

struct TrendsView_Previews: PreviewProvider {
    static var previews: some View {
        TrendsView()
    }
}
